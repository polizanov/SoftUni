function sumArrays(inputArray) {
    let furstElelemnt = Number(inputArray[0]);
    let lastElement = Number(inputArray[inputArray.length - 1]);

    console.log(furstElelemnt + lastElement);
}

sumArrays(['20', '30', '40']);