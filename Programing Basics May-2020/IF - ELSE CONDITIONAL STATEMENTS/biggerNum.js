function biggerNum(numA, numB) {
    numA = Number(numA);
    numB = Number(numB);

    if (numA > numB) {
        console.log(numA);
    } else if (numA < numB) {
        console.log(numB);
    }

}